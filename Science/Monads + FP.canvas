{
	"nodes":[
		{"id":"4c09f54de9417735","type":"text","text":"### Эффекты\n\nЧистая функция:\n- Определена на всём домене\n- Не имеет побочных эффектов (ничего не меняет снаружи)\n- Детерминирована: всегда возвращает один и тот же результат\n\nЭффекты - всё, что нарушает чистоту\n\n| Эффект             | Пример                                       | Монадическая модель |\n| ------------------ | -------------------------------------------- | ------------------- |\n| **Ошибка**         | Деление на ноль                              | `Maybe`, `Either e` |\n| **Nondeterminism** | Вернуть несколько возможных значений         | `[]` (списки)       |\n| **Ввод/вывод**     | Прочитать строку с клавиатуры                | `IO`                |\n| **Состояние**      | Изменение переменной счётчика                | `State s`           |\n| **Логирование**    | Собирать журнал операций                     | `Writer w`          |\n| **Прерывание**     | Прервать выполнение при определённом условии | `Cont`, `Either`    |","x":-580,"y":-248,"width":740,"height":708},
		{"id":"9d628c99865fcc58","type":"text","text":"## 🔐 Как это помогает повысить надёжность LLM\n\n### 1. **Формализация ограничений на поведение**\n\nМожно задать **тип входа → тип вывода**, где:\n\n- вывод должен удовлетворять определённой логике, структуре, формату;\n    \n- отклонения от ожидаемого поведения — **типовая ошибка**.\n    \n\n**Пример**: вместо обычного вывода строки, модель обязана возвращать:\n\n```haskell\nEither Error (AST, Proof)\n```\n\n⟶ в случае отсутствия доказуемой цепочки генерации — ошибка, не галлюцинация.\n\n---\n\n### 2. **Модулярность и интерпретируемость**\n\nLLM можно трактовать как \"чёрный ящик\", а можно:\n\n- построить его как композицию категориальных слоёв (encoding, attention, feedforward);\n    \n- использовать **оптические алгебры** и **оптимизацию как морфизм** — чтобы управлять интерпретацией.\n    \n\nЭто позволяет встроить в архитектуру:\n\n- проверку согласованности слоёв,\n    \n- проверку промежуточных значений (например, логические инварианты).\n    \n\n---\n\n### 3. **Прямое взаимодействие с LLM через типизированные каналы**\n\nПромпт → это просто строка.  \nНо если построить **DSL** поверх категории (или оптики), можно:\n\n- передавать не строки, а **типизированные данные** (`Statement`, `Query`, `ProofRequest`, и т.д.);\n    \n- получать ответы, строго удовлетворяющие типу (`Maybe Theorem`, `Either Error Program`, `Distribution Response`).\n    \n\n**Это не просто \"чёткий промпт\", это — типобезопасный интерфейс.**\n\n---\n\n### 4. **Гарантия семантических инвариантов**\n\nНапример, если ты используешь LLM для генерации кода:\n\n- с категорическим фреймворком можно описать допустимые программы как **алгебраические структуры**;\n    \n- проверить их корректность — **ещё до исполнения**;\n    \n- включить проверки семантической эквивалентности (не просто “парсится ли”, а \"делает ли то, что надо\").\n    \n\n---\n\n## 🔁 Возможный стек:\n\n|Уровень|Идея|Категориальный аналог|\n|---|---|---|\n|Интерфейс|LLM вызывается через типизированный канал|Оптика, линза, интерфейс → морфизм в категории|\n|Проверка|Структура ответа проверяется статически|Тип + алгебра, проверка коммутативности|\n|Поведение|Обучение моделируется через морфизмы|BiCat, обучающие 2-клетки|\n|Контроль|Ответ “собирается” через категориальные правила|Каноническое развёртывание/сворачивание|\n\n---\n\n## ✨ Вывод: это путь к безопасным LLM\n\nЕсли кратко:\n\n- Да, **категориальный фреймворк может повысить надёжность LLM**.\n    \n- Он позволяет **ввести контроль на уровне архитектуры**, а не на уровне “хорошо ли промпт написан”.\n    \n- Он может привести к **типобезопасным интерфейсам генерации и вывода**, где ошибка = **невозможность скомпилировать модель**, а не “ошибся в ответе”.","x":-980,"y":560,"width":1086,"height":1849},
		{"id":"1ec6fca97346382e","type":"text","text":"### Заметки/логи\n\n **24.05**\n\nИнтуиция для монады и алгебры ($G \\times \\_$)\n\n| Классический подход                       | Категориальный подход                              |\n| ----------------------------------------- | -------------------------------------------------- |\n| Группа $G$ действует на $X$ через $G×X→X$ | Монада $T$ действует на $X$ через морфизм $T(X)→X$ |\n| Аксиомы действия                          | Коммутативные диаграммы                            |\n| Конкретное действие                       | Монадическая алгебра (Eilenberg–Moore)             |\n\n```gpt\nСравнение с type safety\n\nКатегориальные диаграммы — это **типобезопасность на уровне поведения**. Не просто: «этот объект имеет нужный тип», а: «все его взаимодействия строго удовлетворяют законам».\nЕсли хочешь, я могу привести **конкретную коммутативную диаграмму** из алгебры действия группы, чтобы закрепить эту аналогию с `class-instance` окончательно.\n```\n\nList - левый сопряжённый к забывающему Mon -> Set, алгебры для List это моноиды\n\n**25.05**\n\nМонады - примеры классов, алгебры - инстансы, гомоморфизмы алгебр - допустимые преобразования инстансов (уважающие структуру), категория Кляйсли - типизированный язык программирования, где все функции могут иметь эффект T.\n\nЭквивариантность: сдвиг; функция (матрица) = функция; сдвиг (матрица)","x":440,"y":-140,"width":820,"height":1020}
	],
	"edges":[]
}